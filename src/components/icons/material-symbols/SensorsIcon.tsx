import { Box } from "@mui/material";

const SensorsIcon = ({ ...props }) => {
  return (
    <Box {...props} className="material-symbols-outlined">
      sensors
    </Box>
  );
};

export { SensorsIcon };
